# خطة إعادة تصميم صفحة التقارير وحل مشكلة الـ Overflow

سنقوم بإعادة تصميم صفحة التقارير (`lib/features/dashboard/reports_screen.dart`) بالكامل لإضافة تجربة مستخدم فاخرة (Premium UI) وتحسين عرض الإحصائيات الرياضية مع حل مشكلة الـ Overflow التي تظهر في الجوالات الذكية.

## سبب المشكلة (Overflow Issue)
تحدث مشكلة الـ `bottom overflowed by 3.7 pixels` بسبب أن شبكة الإحصائيات العامة (`_OverviewGrid`) تستخدم `GridView.builder` بنسبة عرض إلى ارتفاع ثابتة (`childAspectRatio: 1.45`). في بعض الشاشات الصغيرة، لا تكفي المساحة المتاحة لعرض العناصر (أيقونة، تباعد، اسم الإحصائية، قيمة الإحصائية، هوامش) مما يسبب هذا التدفق الزائد.

## التغييرات المقترحة

### 1. تصميم واجهة مستخدم حديثة وبسيطة (Premium Dashboard Design)
* **رأس الصفحة (Header Card)**: بدلاً من الشريط العلوي الافتراضي العادي، سنقوم بوضع كارد علوي أنيق يحتوي على تدرج لوني جذاب (Gradient) يعرض ملخص الإحصائيات العامة مثل "إجمالي الرياضات النشطة" و"إجمالي البطولات" بلمسة عصرية.
* **بطاقات الإحصائيات المحسنة (KPI Cards)**: سنعيد هيكلة بطاقات الشبكة لتستخدم تصميمًا أفقيًا (Row يحتوي على أيقونة ملونة في الخلفية مع عنوان وقيمة الإحصائية بجانبها) بدلاً من التصميم العمودي، لضمان استجابة مرنة (Responsive) وحجم أصغر يمنع حدوث أي Overflow نهائياً.
* **الرسومات البيانية المخصصة (Custom Interactive Visuals)**:
  * سنقوم ببرمجة رسم بياني دائري مخصص (Donut Chart) باستخدام `CustomPainter` لعرض نسب طلبات التسجيل (مقبولة، معلقة، مرفوضة) بشكل دائري جذاب مع تفاصيل موضحة.
  * سنقوم بتصميم تمثيل شريطي مخصص وممتد (Horizontal Bar Chart) لعرض توزيع الفرق بحسب الكليات مع أيقونات ملونة ومؤشرات تقدم متدرجة (Gradients).
  * سنستخدم مؤشرات دائرية للمباريات المكتملة والمستقبلية.

### 2. الملفات المتأثرة

#### [MODIFY] [reports_screen.dart](file:///e:/uniproj/lib/features/dashboard/reports_screen.dart)
سنستبدل محتويات هذا الملف لتشمل:
1. واجهة مستخدم تفاعلية وجذابة مع خلفيات متدرجة وظلال ناعمة (Soft Shadows).
2. كلاس `DonutChartPainter` لرسم المخطط الدائري لطلبات التسجيل.
3. معالجة مشكلة الـ Overflow عن طريق تعديل شبكة الإحصائيات لتستخدم `SliverGridDelegateWithFixedCrossAxisCount` مع `childAspectRatio` مناسب أو باستخدام هيكلية مرنة لا تعتمد على الارتفاعات الثابتة.

---

## خطة التحقق والتحبير (Verification Plan)

### التحقق اليدوي
1. تشغيل التطبيق على محاكي الجوال أو جهاز حقيقي والوصول لصفحة التقارير.
2. التحقق من اختفاء خطأ الـ `bottom overflowed`.
3. التحقق من أن الواجهة تظهر بشكل رائع وتقدم إحصائيات تفاعلية سهلة القراءة.
4. تجربة سحب الشاشة للتحديث (Pull-to-refresh) والتأكد من تحديث البيانات بشكل سليم.
